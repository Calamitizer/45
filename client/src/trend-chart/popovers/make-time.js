(function() {
    'use strict';

    const moment = require('moment');

    var makeTime = function(str) { // moment can parse lots of different formats
        var time = moment(s);
        return time;
    };

    module.exports = makeTime;

}());
